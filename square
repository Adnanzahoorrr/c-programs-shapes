#include <stdio.h>
int main() {
    float side, area, perimeter;
    printf("Enter side of square: ");
    scanf("%f", &side);
    area = side * side;
    perimeter = 4 * side;
    printf("Area: %.2f\nPerimeter: %.2f\n", area, perimeter);
    return 0;
}
